class PostType {
  constructor(id, title, description, tags, fields) {
    this.id = id;
    this.title = title;
    this.description = description;
    this.tags = tags;
    this.fields = fields;
  }
}

export default PostType;
